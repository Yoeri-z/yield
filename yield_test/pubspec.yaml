name: yield_test
description: "Testing utilities for the yield_state package"
version: 0.0.2
homepage: https://github.com/Yoeri-z/yield
repository: https://github.com/Yoeri-z/yield

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  yield_state: ^0.0.2

dev_dependencies:


flutter: